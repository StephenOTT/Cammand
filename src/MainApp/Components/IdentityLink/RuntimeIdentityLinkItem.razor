@if (Items != null)
{
    @if (Items.Any())
    {
        <MudText>@string.Join(",", Items)</MudText>
    }
    else
    {
        <MudText><i>None</i></MudText>
    }
}
else
{
    <MudText>Loading...</MudText>
}


@code {
    
    [Parameter]
    public IEnumerable<string> Items { get; set; }

}